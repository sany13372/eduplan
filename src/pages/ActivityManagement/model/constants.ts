import { CommonInfo, EduGridElement } from '@src/pages/ActivityManagement/model/types';

const defaultObj: CommonInfo = { id: 'all', caption: 'Все' };
export const defaultEduGridElement: EduGridElement = defaultObj;
