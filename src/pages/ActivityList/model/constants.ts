import { CommonInfo, EduGridElement } from '@src/pages/ActivityList/model/types';

const defaultObj: CommonInfo = { id: 'all', caption: 'Весь срок освоения' };
export const defaultEduGridElement: EduGridElement = defaultObj;
